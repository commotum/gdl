# 3-READOUT

## Result

- Added `scripts/archive-x-dashboard` with one-shot and watch modes over the
  atomic snapshot.
- The default per-user block is seven accomplishment-first lines and is clipped
  to terminal width. The 80-column fixture contains no ANSI in plain mode.
- Multiple users receive equal repeated blocks. Missing, malformed, and stale
  snapshots fail visibly without archive access.
- Unicode separators/ellipsis are capability-dependent; redirected output is
  append-only plain text.
